language: rust
cache: cargo
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
    - rust: beta
script:
- rustup component add rustfmt-preview
- cargo fmt --all -- --check
- cargo test --verbose
- cargo doc
